# ── Ollama ──
OLLAMA_PORT=11434

# ── Vector ──
VECTOR_PORT=8081
VECTOR_API_PORT=8686

# ── Message Broker ──
# MESSAGE_BROKER로 kafka 또는 redpanda 둘 중 하나 선택 가능
MESSAGE_BROKER=kafka

KAFKA_HOST=localhost
KAFKA_PORT=9092
KAFKA_EXTERNAL_PORT=19092
KAFKA_CONTROLLER_PORT=9093
KAFKA_TOPIC=test_topic
KAFKA_GROUP_ID=shire-guard-enrichment

REDPANDA_HOST=localhost
REDPANDA_PORT=29092

# ── OpenSearch ──
OPENSEARCH_PORT=9200
OPENSEARCH_PERF_PORT=9600
OPENSEARCH_INITIAL_ADMIN_PASSWORD=ChangeMe2026!
OPENSEARCH_DASHBOARDS_PORT=5601

# ── ClickHouse ──
CLICKHOUSE_HTTP_PORT=8123
CLICKHOUSE_NATIVE_PORT=9000
CLICKHOUSE_DB=shire
CLICKHOUSE_USER=shire
CLICKHOUSE_PASSWORD=ChangeMe2026!

# ── Grafana ──
GRAFANA_PORT=3000

# ── PostgreSQL ──
POSTGRES_PORT=5432
POSTGRES_DB=shire
POSTGRES_USER=shire
POSTGRES_PASSWORD=ChangeMe2026!

# ── Redis ──
REDIS_PORT=6379
REDIS_PASSWORD=ChangeMe2026!

# ── Dragonfly (Redis 대체 테스트용) ──
DRAGONFLY_PORT=6380
DRAGONFLY_PASSWORD=ChangeMe2026!

# ── Prometheus ──
PROMETHEUS_PORT=9090

# ── cAdvisor ──
CADVISOR_PORT=8082