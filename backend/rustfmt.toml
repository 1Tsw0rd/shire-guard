# 프로젝트 edition과 동일하게 맞춤 (Cargo.toml의 edition = "2024")
edition = "2024"
 
# 한 줄 최대 길이. 기본값(100)을 그대로 명시적으로 설정
max_width = 100
 
# 줄바꿈 문자를 Unix(LF)로 통일 (Windows에서 작업해도 CRLF로 안 바뀌게 고정)
newline_style = "Unix"
 